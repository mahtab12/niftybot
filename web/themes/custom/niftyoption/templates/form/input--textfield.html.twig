{#
/**
 * @file
 * Auth text field (login / register only).
 */
#}
{% if niftyoption_auth %}
  <input{{ attributes.addClass('form-control') }} />{{ children }}
{% else %}
  <input{{ attributes }} />{{ children }}
{% endif %}
