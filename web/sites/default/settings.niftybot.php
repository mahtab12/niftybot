<?php

/**
 * @file
 * NiftyBot site settings included from settings.php.
 */

// KYC documents and other sensitive uploads (outside the web root).
$settings['file_private_path'] = dirname($app_root) . '/private';
